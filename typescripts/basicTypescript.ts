import scriptTitle from '../helpers/scriptTitle'



const onlyAcceptsNumbers = (passedNumber: number) => {
  return passedNumber
}

const basicTypescript = () => {
  scriptTitle('Exercise to minimize the code needed to complete the FizzBuzz challenge')
  console.log('eeef')
  onlyAcceptsNumbers(2)

}

export default basicTypescript